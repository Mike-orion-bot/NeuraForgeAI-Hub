version: "3.8"

services:
  web:
    build: .
    ports:
      - "10000:10000"
    environment:
      - PORT=10000
      - SECRET_KEY=b33f_s3cr3t_k3y_for_affiliates_and_monetization_123456789
      - ACCESS_TOKEN_EXPIRE_MINUTES=1440
      - DATABASE_URL=sqlite:///./database.db
    volumes:
      - .:/app
